# Static viewer assets

This folder contains the generated public site served by the Cloudflare worker.

The DIY simulator is split across:

- `index.html` for controls and canvases.
- `app.js` for navigation and mask drawing.
- `sim.js` for browser simulation.
- `styles.css` for layout.

The source templates live in `tools/static_templates/`.
